# TECH_DEBT.md — технический долг

Открытые технические вопросы, которые сейчас не блокируют работу. Закрытые пункты — в `DESIGN.md`, раздел 11.
Формат: **ID. Что** — чего касается, где лежит, что сделать. Пометки те же, что в `DESIGN.md`.
Закрыл пункт — перенеси строку в `DESIGN.md` (раздел 11, «Закрыто») с датой.

---

## Критичное

**TD-01. Переворот Залужного (`russia.76`)** **[КРИТИЧНО]**
- Что: событие TFR «Ukraine's government collapses». Залужный становится лидером `military_junta`, выборы запрещены, армия получает `UKR_collapsing_military`, национализм +26%. По описанию автора срабатывает около 30% при падении Киева.
- Где: наша копия лежит в `events/TFR_events_SOV_UKR.txt`, от TFR отличается одним условием: национализм в UKR > 40% (попытка автора сделать его редким). Оригинал — в `TFR_events_SOV.txt` мода TFR.
- Проблема: ID события дублируется с TFR, и неизвестно, какая версия побеждает при загрузке. Кто вызывает событие, в наших референсах не видно.
- Касается: 3-го акта (война), связанных событий и фокусов других стран. Решение [РЕШЕНО]: отключить или переработать (`DESIGN.md`, 9.4).
- Сделать: 1) автор ищет `russia.76` по папке TFR (файл и условие вызова); 2) автор присылает строку из `error.log` про дубль; 3) после этого — отключить через условие в нашей копии или флагом в месте вызова.

**TD-02. Перехват `on_capitulation`** **[КРИТИЧНО]**
- Что: раздел Украины при капитуляции (Новороссия, Малороссия, Карпатская Русь).
- Касается: блока 4 (оккупация и подполье, `DESIGN.md`, 9.5 и 10.1).
- Сделать: только изучить, ничего не менять до согласования.

## Проверить по `error.log`

**TD-03. `UKR_gdp_fix`**
- Идея, которая выдаётся в `history/countries/UKR - Ukraine.txt` (стартовые идеи). У нас не объявлена, вероятно, лежит в общих файлах TFR. Если в логе ошибка — найти в TFR или убрать.

**TD-04. `UKR_russian_ghetto`**
- Динамический модификатор, который выдаётся областям 1283, 853, 1059 в событии `ukraine.6` (базовое событие TFR). У нас не объявлен, вероятно, лежит в TFR.

**TD-05. Тексты событий `ukraine.6`–`ukraine.19`**
- Базовые события TFR, скопированы автором. Их ключей (`.t`, `.d`) нет в нашей локализации на обоих языках.
- Риск: наш `TFR_country_localisation_UKR_l_*.yml` заменяет одноимённый файл TFR. Если тексты лежали там, они потеряны.
- Сделать: открыть в игре, например, `ukraine.6`. Если вместо текста видны ключи — скопировать тексты из TFR.

**TD-06. Идея `supply_crisis`**
- Проверяется в миссии `UKR_war_mood_monthly_tick` (`common/decisions/TFR_decisions_UKR_war.txt`). У нас не объявлена. Если её нет и в TFR, условие мёртвое — заменить на реальную проверку снабжения.

**TD-07. Вызовы событий TFR**
- `germany.*`, `nato.*`, `news.*`, `moldova.2`, `syria.25`, `russia.*` из наших решений и событий. Предполагаются в TFR, сверить по логу, что все существуют.

## Отложенный контент (вызовы отключены `# TODO`)

**TD-08. События `ukraine.801`, `ukraine.802`**
- Вызывались из фокусов `UKR_military_budget_expansion` и `UKR_shadow_war_capabilities`, но не существуют. Диапазон 800–899 — ГУР и добровольцы.
- Сделать: написать события или удалить закомментированные вызовы.

**TD-09. `ukr_news.15`, `ukraine_news.1`**
- `ukr_news.15` — в решении разведданных НАТО (`TFR_decisions_UKR.txt`, ветка ЛДПР). `ukraine_news.1` — в событии `ukraine.105`. Namespace `ukr_news` не существует, `ukraine_news` запланирован.
- Сделать: при создании `ukraine_news` перенести туда и раскомментировать. Остальные закомментированные `ukr_news.1/2/3/24` — туда же.

## Мелочи и порядок

**TD-10. Потолок настроя 120**
- Миссия `UKR_war_mood_monthly_tick` ограничивает `UKRhate` и `UKRwarfatigue` значением 120, а по `CLAUDE.md` шкала 0–100 (у GUI есть уровень «100+»). Решить, какой потолок правильный, и привести к одному.

**TD-11. Дубли ключей в `TFR_characters_l_*.yml`**
- Персонажи других стран в копии файла TFR. Не трогаем; если мешают — чистить отдельным проходом.

**TD-12. Папка `ukraine_2026/`**
- Остаток старта 2026, не используется (`CLAUDE.md`). Решить: удалить или вынести из игровых путей.

**TD-13. Аудит `_tools/audit.py`**
- Считает скрытые события (`hidden = yes`) без локализации ошибкой, например `ukr_ui.*`. Научить пропускать их.

**TD-14. Общий аудит (бывшие T-02–T-05)**
- Иконки и `prerequisite` фокусов, закомментированные вызовы, сверка реестра ID с кодом — по ходу работы.

**TD-15. Перестройка вида дерева фокусов (блок 1)**
- Цель [РЕШЕНО]: дерево ветвится в середине и сходится в конце на «Золотой век» (бутылочное горлышко). Сейчас четыре независимых столбца (x = 3 / 10 / 20 / 30), политическая цепочка скрыто открывает остальное через `available`.
- Касается: координат `x`/`y` и `relative_position_id` всех 53 фокусов, скрытых `available` (заменить на явные `prerequisite`, чтобы связи были видны), необязательной ветки склонностей (сейчас столбец справа от политики, x = 5).
- Сюда же: какие фокусы считаются концом политической, экономической, военной ветки и декоммунизации — это требования «Золотого века» (`DESIGN.md`, 4.3). Черновик: «Территориальная оборона», «Инвестиции в инфраструктуру», финал любой доктрины, «Реформа образования».
- Бюджет: самый длинный обязательный путь до «Золотого века» должен укладываться примерно в 200 единиц (сейчас всё дерево — 195, с необязательной веткой — 204).
- Сделать: когда автор скажет, какие фокусы обязательны, и утвердит новый вид дерева.

**TD-16. Названия партий Украины в TFR**
- Нужно знать, какая партия стоит за каждой идеологией (`social_liberal` — «Слуга народа», `conservative` — «Батькивщина» и, видимо, Порошенко, `market_liberal`, `social_democrat` и т. д.). Названия, вероятно, в локализации оригинального TFR.
- Касается: текстов событий 2023 года (`ukraine_politics` 100–125), выборов и союза. Сейчас в текстах партии названы по реальным именам только там, где это однозначно («Слуга народа», «Батькивщина»).
- Сделать: автор присылает строки из TFR или добавляет названия в наш проект (`set_party_name` или `replace/`).

**TD-17. Кто и когда вызывает `ukraine.13`**
- Событие президентских выборов 2024 (`events/TFR_events_UKR.txt`) вызывается откуда-то из TFR — не из наших файлов и не из референса SOV. Дата вызова неизвестна (по дизайну — март – апрель 2024).
- Касается: блока 2 (кампания и второй тур должны совпасть с выборами).
- Сделать: автор ищет `ukraine.13` по папке TFR и присылает место вызова и условие.

