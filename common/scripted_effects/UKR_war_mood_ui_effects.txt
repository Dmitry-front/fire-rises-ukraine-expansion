# Обновление прогресс-баров
UKR_update_war_mood_ui = {
    # Рассчитываем процент заполнения для ненависти (0-100)
    set_variable = { var = UKRhate_percent value = UKRhate }
    divide_variable = { var = UKRhate_percent value = 100 }
    
    # Рассчитываем процент заполнения для усталости (0-100)
    set_variable = { var = UKRwarfatigue_percent value = UKRwarfatigue }
    divide_variable = { var = UKRwarfatigue_percent value = 100 }
}
